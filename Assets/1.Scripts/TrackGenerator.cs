using UnityEngine;

public class TrackGenerator : MonoBehaviour {
	
	private void Start() {
		// Segment.Create(transform, "Segment", new SegmentInputData());
		// LaneGenerator.Instance.Generate(transform, new LaneData { type = LaneType.SideWalk, length = 80 });
	}
	
}
